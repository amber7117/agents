# copy to .env and tweak
PORT=4000
JWT_SECRET=change_me
DATABASE_URL="file:../data.sqlite" # prisma (relative to packages/db)
CORS_ORIGIN=http://localhost:5173

# AI Encryption Key (for API credentials)
# 用于加密用户的 API Key，至少 32 字节
SECRET_ENC_KEY=change_this_to_32_bytes_min_for_production

# AI Provider URLs (optional, use defaults if not set)
XAI_BASE_URL=https://api.x.ai/v1
GEMINI_BASE_URL=https://generativelanguage.googleapis.com