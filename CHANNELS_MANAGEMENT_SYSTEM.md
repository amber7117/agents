# 频道管理系统 (Channels Management)

## ✅ 功能概述

全新的频道管理系统，允许用户通过 UI 界面添加、管理和连接多个通讯频道（如 WhatsApp）。

## 🎯 功能特性

### 1. 频道列表管理
- ✅ 网格布局展示所有频道
- ✅ 实时显示连接状态（已连接/未连接/连接中）
- ✅ 频道卡片带悬停动画效果
- ✅ 空状态友好提示

### 2. 添加频道
- ✅ 右上角"添加频道"按钮
- ✅ 下拉菜单选择频道类型
- ✅ 当前支持：WhatsApp
- ✅ 自动生成频道名称和 ID

### 3. 连接管理
- ✅ **未连接状态**：显示"连接"按钮
- ✅ **连接中状态**：按钮变灰，显示"连接中..."
- ✅ **已连接状态**：显示"断开连接"按钮
- ✅ 点击连接按钮 → 弹出 QR 码扫描界面
- ✅ 扫码成功 → 自动关闭弹窗，状态更新为"已连接"

### 4. QR 码扫描界面
- ✅ 全屏遮罩弹窗
- ✅ 优雅的卡片设计
- ✅ 实时生成 QR 码
- ✅ 使用说明指引
- ✅ 关闭按钮（右上角 X）

### 5. 断开连接
- ✅ 已连接频道显示"断开连接"按钮
- ✅ 点击后发送 `wa.stop` 事件
- ✅ 状态实时更新为"未连接"
- ✅ 可重新连接

### 6. 删除频道
- ✅ 每个频道卡片右下角有删除按钮（🗑️）
- ✅ 删除前弹出确认对话框
- ✅ 删除后从列表中移除

## 📂 文件结构

```
apps/web/src/
├── pages/
│   └── Channels.tsx          # 频道管理页面（新建）
├── components/
│   └── Sidebar.tsx            # 侧边栏（已更新）
└── main.tsx                   # 路由配置（已更新）
```

## 🎨 UI 设计

### 页面布局
```
┌────────────────────────────────────────────────┐
│  📡 频道管理                    [+ 添加频道]    │
│  管理您的通讯频道连接                           │
└────────────────────────────────────────────────┘

┌──────────────┐  ┌──────────────┐  ┌──────────────┐
│  📱 WhatsApp 1│  │  📱 WhatsApp 2│  │  📱 WhatsApp 3│
│  ● 已连接     │  │  ● 未连接     │  │  ● 连接中...  │
│              │  │              │  │              │
│ [断开连接] 🗑️│  │ [连接] 🗑️    │  │ [连接中...] 🗑️│
└──────────────┘  └──────────────┘  └──────────────┘
```

### 频道卡片
- **图标**：WhatsApp 绿色渐变图标 📱
- **状态指示器**：带呼吸动画的圆点
- **颜色编码**：
  - 🟢 已连接：`#4facfe`（蓝色）
  - 🔴 未连接：`#ff6b6b`（红色）
  - 🟡 连接中：`#ffa502`（橙色）

### QR 码弹窗
```
┌────────────────────────────────────────┐
│                   ✕                    │
│         📱                             │
│    扫描二维码连接                       │
│                                        │
│  ┌──────────────────────────┐         │
│  │                          │         │
│  │      [QR CODE HERE]      │         │
│  │                          │         │
│  └──────────────────────────┘         │
│                                        │
│  📖 扫描步骤                           │
│  1. 打开 WhatsApp                      │
│  2. 点击菜单                           │
│  3. 选择"关联设备"                     │
│  4. 扫描二维码                         │
└────────────────────────────────────────┘
```

## 🔄 状态流转

### 完整生命周期

```
[添加频道]
    ↓
未连接 (disconnected)
    ↓ [点击连接按钮]
连接中 (connecting) → [弹出 QR 码]
    ↓ [用户扫码]
已连接 (connected)
    ↓ [点击断开按钮]
未连接 (disconnected)
    ↓ [点击删除]
[频道被删除]
```

### 状态说明

| 状态 | 显示 | 按钮 | 操作 |
|------|------|------|------|
| `disconnected` | 🔴 未连接 | 🔗 连接 | 点击显示 QR 码 |
| `connecting` | 🟡 连接中... | 🔄 连接中... (禁用) | 等待扫码 |
| `connected` | 🟢 已连接 | 🔌 断开连接 | 发送 `wa.stop` |

## 🔌 Socket.IO 事件

### 前端发送事件

```typescript
// 断开连接
socket.emit('wa.stop');
```

### 前端监听事件

```typescript
// QR 码生成
socket.on('wa.qr', ({ qr }) => {
  // 渲染 QR 码到 canvas
});

// 连接成功
socket.on('wa.ready', () => {
  // 更新状态为 connected
  // 关闭 QR 码弹窗
});

// 断开成功
socket.on('wa.stopped', () => {
  // 更新状态为 disconnected
});
```

## 💾 数据持久化

### localStorage 存储

频道数据保存在浏览器 localStorage 中：

```typescript
interface Channel {
  id: string;              // 唯一标识
  type: 'whatsapp';        // 频道类型
  name: string;            // 显示名称
  phoneNumber?: string;    // 电话号码（可选）
  status: 'connected' | 'disconnected' | 'connecting';
  createdAt: Date;         // 创建时间
}

// 存储 key
localStorage.setItem('channels', JSON.stringify(channels));
```

### 数据示例

```json
[
  {
    "id": "whatsapp-1699876543210",
    "type": "whatsapp",
    "name": "WhatsApp 1",
    "status": "connected",
    "createdAt": "2025-11-07T08:00:00.000Z"
  },
  {
    "id": "whatsapp-1699876598765",
    "type": "whatsapp",
    "name": "WhatsApp 2",
    "status": "disconnected",
    "createdAt": "2025-11-07T08:05:00.000Z"
  }
]
```

## 🎯 使用场景

### 场景 1: 添加第一个 WhatsApp 账号

1. 用户点击 Sidebar 的 "📡 Channels" 菜单
2. 看到空状态提示："还没有添加任何频道"
3. 点击右上角 "添加频道" 按钮
4. 从下拉菜单选择 "📱 WhatsApp"
5. 新频道卡片出现，状态为 "未连接"
6. 点击 "🔗 连接" 按钮
7. 弹出 QR 码扫描界面
8. 使用手机 WhatsApp 扫码
9. 扫码成功，弹窗自动关闭
10. 频道状态更新为 "✅ 已连接"

### 场景 2: 管理多个 WhatsApp 账号

```
频道 1 (已连接)    → 用于客服团队
频道 2 (已连接)    → 用于营销推广
频道 3 (未连接)    → 备用账号
```

### 场景 3: 临时断开某个账号

1. 找到已连接的频道卡片
2. 点击 "🔌 断开连接" 按钮
3. 后端收到 `wa.stop` 事件
4. Socket 断开，会话清理
5. 状态更新为 "未连接"
6. 稍后可以重新连接（点击 "🔗 连接"）

### 场景 4: 删除不需要的频道

1. 点击频道卡片右下角的 "🗑️" 按钮
2. 弹出确认对话框："确定要删除此频道吗？"
3. 确认后，频道从列表中移除
4. 如果频道已连接，建议先断开再删除

## 🔧 技术实现

### 核心组件

#### 1. Channels.tsx

```typescript
// 状态管理
const [channels, setChannels] = useState<Channel[]>([]);
const [showQRModal, setShowQRModal] = useState(false);
const [currentChannelId, setCurrentChannelId] = useState<string | null>(null);

// Socket.IO 连接
const socketRef = useRef<Socket | null>(null);

// 初始化 Socket 事件监听
useEffect(() => {
  const sock = io(API_URL, { auth: { token } });
  
  sock.on('wa.qr', handleQR);
  sock.on('wa.ready', handleReady);
  sock.on('wa.stopped', handleStopped);
  
  return () => sock.close();
}, []);
```

#### 2. QR 码生成

```typescript
import QRCode from 'qrcode';

sock.on('wa.qr', async (payload) => {
  await QRCode.toCanvas(canvasRef.current, payload.qr, {
    width: 300,
    margin: 2,
    color: { dark: '#000000', light: '#FFFFFF' }
  });
});
```

#### 3. 状态更新

```typescript
const updateChannelStatus = (id: string, status: Channel['status']) => {
  const updated = channels.map(ch => 
    ch.id === id ? { ...ch, status } : ch
  );
  saveChannels(updated);
};
```

## 📱 响应式设计

### 桌面端 (>1200px)
```css
grid-template-columns: repeat(auto-fill, minmax(350px, 1fr));
```
- 每行 3-4 个频道卡片

### 平板端 (768px - 1200px)
- 每行 2 个频道卡片

### 移动端 (<768px)
- 每行 1 个频道卡片
- QR 码弹窗全屏显示

## 🎨 动画效果

### 1. 卡片悬停
```typescript
onMouseEnter={(e) => {
  e.currentTarget.style.transform = 'translateY(-4px)';
  e.currentTarget.style.boxShadow = '0 8px 24px rgba(0, 0, 0, 0.3)';
}}
```

### 2. 状态指示器呼吸动画
```css
@keyframes pulse {
  0%, 100% { opacity: 1; }
  50% { opacity: 0.5; }
}
```

### 3. 按钮悬停
- 上浮效果 (`translateY(-2px)`)
- 阴影增强

## 🔒 安全性

### 1. 权限验证
- ✅ Socket.IO 中间件验证 JWT token
- ✅ 每个用户只能管理自己的频道

### 2. 数据隔离
- ✅ localStorage 按用户存储
- ✅ 后端 `uid` 从 token 解析

### 3. 会话管理
- ✅ 每个频道独立的 `channelId`
- ✅ 服务器端维护 `uid` → `socket` 映射

## 🐛 错误处理

### 1. QR 码生成失败
```typescript
try {
  await QRCode.toCanvas(canvasRef.current, payload.qr);
} catch (error) {
  console.error('QR code generation failed:', error);
  // 显示错误提示
}
```

### 2. 连接超时
- 连接中状态超过 60 秒自动重置
- 提示用户重新尝试

### 3. Socket 断线
```typescript
sock.on('disconnect', () => {
  // 更新所有频道状态为 disconnected
  // 显示重连提示
});
```

## 🚀 后续改进

### 短期计划

1. **电话号码显示**
   - 连接成功后获取绑定的电话号码
   - 显示在频道卡片上

2. **连接统计**
   - 显示连接时长
   - 消息发送/接收数量

3. **批量操作**
   - 全部连接
   - 全部断开

### 长期计划

1. **更多频道类型**
   - Telegram Bot
   - Facebook Messenger
   - LINE
   - WeChat

2. **频道分组**
   - 客服组
   - 营销组
   - 测试组

3. **权限管理**
   - 团队成员共享频道
   - 角色权限控制

4. **健康监控**
   - 连接状态监控
   - 异常告警
   - 自动重连

## 📊 性能优化

### 1. 虚拟滚动
- 当频道数量 > 50 时启用
- 只渲染可见区域的卡片

### 2. 懒加载
- QR 码按需生成
- 图标按需加载

### 3. 缓存策略
- localStorage 缓存频道列表
- 减少不必要的重新渲染

## 🎓 开发指南

### 添加新的频道类型

1. 更新 `Channel` 接口类型
```typescript
type: 'whatsapp' | 'telegram' | 'messenger';
```

2. 在下拉菜单中添加选项
```tsx
<button onClick={() => handleAddChannel('telegram')}>
  <span>🤖</span>
  <div>Telegram Bot</div>
</button>
```

3. 实现对应的连接逻辑
```typescript
if (channel.type === 'telegram') {
  // Telegram 特定的连接逻辑
}
```

### 自定义频道卡片样式

修改 `Channels.tsx` 中的卡片样式：
```typescript
background: channel.type === 'telegram' 
  ? 'linear-gradient(135deg, #0088cc 0%, #229ED9 100%)'
  : 'linear-gradient(135deg, #25D366 0%, #128C7E 100%)'
```

---

**实现完成**: 2025-11-07  
**文件修改**: 
- `apps/web/src/pages/Channels.tsx` (新建)
- `apps/web/src/components/Sidebar.tsx` (添加菜单项)
- `apps/web/src/main.tsx` (添加路由)

**状态**: ✅ 功能完整，可以测试
