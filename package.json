{
  "name": "wa-monorepo",
  "private": true,
  "packageManager": "pnpm@9.12.0",
  "workspaces": [
    "packages/*",
    "packages/*/*",
    "apps/*"
  ],
  "scripts": {
    "dev": "concurrently -n conn,api,web -c yellow,blue,green 'pnpm -F @pkg/connectors-whatsapp dev' 'pnpm -F @app/api dev' 'pnpm -F @app/web dev'",
    "dev:connectors": "pnpm -F @pkg/connectors-core dev & pnpm -F @pkg/connectors-whatsapp dev",
    "dev:api": "cd apps/api && bun run dev",
    "dev:web": "cd apps/web && bun run dev",
    "build": "pnpm -F @pkg/connectors-core build && pnpm -F @pkg/connectors-whatsapp build && pnpm -F @app/api build && pnpm -F @app/web build",
    "prisma:generate": "bun --filter @pkg/db prisma:generate",
    "prisma:migrate": "bun --filter @pkg/db migrate"
  },
  "devDependencies": {
    "@types/bcryptjs": "^3.0.0",
    "concurrently": "^9.0.1",
    "tsup": "^8.5.0"
  },
  "dependencies": {
    "typescript": "^5.9.3"
  }
}